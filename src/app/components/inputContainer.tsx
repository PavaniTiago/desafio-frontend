import { styled } from 'styled-components'

export const InputContainer = styled.div`
  position: relative;
  margin-top: 2em;

  svg {
    position: absolute;
    right: 15px;
    top: 50%;
    transform: translateY(-50%);
  }
`
